Steps to run the application:
1) Start the mongoDB in your local machine. create a db with name as "messageApp". and inside that db create a collections called messages.
2) GO to backend folder and run this command. node server.js
3) Now open the run me folder and click index.html

Hope it works!
